# Generated by scripts/setup.sh — never commit private keys or per-host configs.
keys/
configs/

# Per-agent installed deps.
agents/*/node_modules/

# Local secrets for docker compose (ANTHROPIC_API_KEY, etc.). Check in
# .env.example with placeholders; never commit the real file.
.env
